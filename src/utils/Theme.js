import { createTheme } from "@mui/material";

const Theme = createTheme({
    palette: {
        primary: {
            main: '#003049',
        },
        secondary: {
            main: '#d62828'
        },
        type: 'dark'
    },
});
export default Theme;